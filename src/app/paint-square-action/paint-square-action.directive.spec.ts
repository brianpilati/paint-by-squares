import { PaintSquareActionDirective } from './paint-square-action.directive';

describe('PaintSquareActionDirective', () => {
  it('should create an instance', () => {
    const directive = new PaintSquareActionDirective();
    expect(directive).toBeTruthy();
  });
});
